# The POS currently runs entirely against the in-browser fake account in
# src/data/fake-account/, so no variables are required to run it.
#
# Once api-vista exists, point the client at it here.
VITE_API_BASE_URL=https://api.vistahub.my
